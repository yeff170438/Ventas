<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Panel Administrador</title>
  <link href="https://fonts.googleapis.com/css2?family=Roboto&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: 'Roboto', sans-serif;
    }

    html, body {
      height: 100%;
      overflow: hidden;
    }

    body {
      display: flex;
      background: linear-gradient(to right, #e0eafc, #cfdef3);
      color: #2c3e50;
    }

    .sidebar {
      width: 250px;
      background-color: #34495e;
      padding: 30px 0;
      color: white;
      display: flex;
      flex-direction: column;
      align-items: center;
    }

    .sidebar h2 {
      margin-bottom: 40px;
      font-size: 22px;
      font-weight: 700;
    }

    .sidebar a {
      display: block;
      width: 100%;
      padding: 15px 25px;
      color: white;
      text-decoration: none;
      font-size: 16px;
      transition: background 0.3s, padding-left 0.3s;
    }

    .sidebar a:hover, .sidebar a.active {
      background-color: #1abc9c;
      padding-left: 35px;
    }

    .main {
      flex-grow: 1;
      height: 100vh;
      display: flex;
      flex-direction: column;
    }

    .main h1 {
      font-size: 32px;
      margin: 30px;
      font-weight: 600;
    }

    .content-area {
      flex-grow: 1;
      padding: 0 30px 30px 30px;
    }

    iframe {
      width: 100%;
      height: 100%;
      border: none;
      border-radius: 10px;
    }
  </style>
</head>
<body>

  <div class="sidebar">
    <h2>Administrador</h2>
    <a href="#" onclick="loadContent('/producto')"><i class="fas fa-boxes"></i> Productos</a>
    <a href="#" onclick="loadContent('/usuarios')"><i class="fas fa-users-cog"></i> Usuarios</a>
    <a href="/reporte"><i class="fas fa-chart-line"></i> Reporte de Ventas</a>
    <a href="/menu"><i class="fas fa-sign-out-alt"></i> Salir</a>
  </div>

  <div class="main">
    
    <div class="content-area">
      <iframe id="adminFrame" src="/producto"></iframe>
    </div>
  </div>

  <script>
    function loadContent(url) {
      document.getElementById('adminFrame').src = url;
    }
  </script>

</body>
</html>
